apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: pantrybot-tls
spec:
  secretName: pantrybot-tls
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
    - pantrybot.cwnel.com
