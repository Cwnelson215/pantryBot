<!DOCTYPE html>
<html lang="en" class="light">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= typeof title !== 'undefined' ? title + ' | ' : '' %>Pantry Bot</title>
  <link rel="icon" type="image/png" href="/pantrybot.png">
  <script src="https://cdn.tailwindcss.com"></script>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
  <script>
    tailwind.config = {
      darkMode: 'class',
      theme: {
        extend: {
          fontFamily: { sans: ['Inter', 'system-ui', 'sans-serif'] },
          colors: {
            primary: { DEFAULT: '#6366F1', hover: '#4F46E5', light: '#818CF8', 50: '#EEF2FF' },
            surface: { light: '#FFFFFF', dark: '#1F2937' },
          }
        }
      }
    }
  </script>
  <style type="text/tailwindcss">
    @layer components {
      /* Flash Messages */
      .flash { @apply p-4 rounded-lg mb-4 border-l-4 text-sm; }
      .flash-success { @apply bg-emerald-50 text-emerald-800 border-emerald-500 dark:bg-emerald-900/30 dark:text-emerald-300 dark:border-emerald-400; }
      .flash-error { @apply bg-red-50 text-red-800 border-red-500 dark:bg-red-900/30 dark:text-red-300 dark:border-red-400; }
      .flash-info { @apply bg-blue-50 text-blue-800 border-blue-500 dark:bg-blue-900/30 dark:text-blue-300 dark:border-blue-400; }
      .flash-warning { @apply bg-amber-50 text-amber-800 border-amber-500 dark:bg-amber-900/30 dark:text-amber-300 dark:border-amber-400; }

      /* Progress Bars */
      .progress-bar { @apply bg-gray-100 dark:bg-gray-700 rounded-lg overflow-hidden h-6 mb-2; }
      .progress-fill { @apply h-full flex items-center justify-center text-xs text-white font-semibold rounded-lg transition-all duration-300; }
      .progress-calories .progress-fill { background: #6366F1; }
      .progress-protein .progress-fill { background: #10B981; }
      .progress-carbs .progress-fill { background: #F59E0B; }
      .progress-fat .progress-fill { background: #EF4444; }
      .progress-fiber .progress-fill { background: #8B5CF6; }
      .progress-sugar .progress-fill { background: #EC4899; }
      .progress-sodium .progress-fill { background: #6B7280; }
      .progress-iron .progress-fill { background: #92400E; }
      .progress-calcium .progress-fill { background: #9CA3AF; }
      .progress-vitaminD .progress-fill { background: #F59E0B; }
      .progress-potassium .progress-fill { background: #059669; }
      .progress-vitaminC .progress-fill { background: #EA580C; }

      /* Badges */
      .badge { @apply inline-block px-2.5 py-0.5 rounded-full text-xs font-semibold; }
      .badge-success { @apply bg-emerald-100 text-emerald-700 dark:bg-emerald-900/40 dark:text-emerald-300; }
      .badge-warning { @apply bg-amber-100 text-amber-700 dark:bg-amber-900/40 dark:text-amber-300; }
      .badge-danger { @apply bg-red-100 text-red-700 dark:bg-red-900/40 dark:text-red-300; }

      /* Tags */
      .tag { @apply inline-block px-2.5 py-1 rounded-full text-xs font-medium bg-gray-100 text-gray-600 dark:bg-gray-700 dark:text-gray-300 border border-gray-200 dark:border-gray-600; }

      /* Modal */
      .modal-tabs { @apply flex border-b-2 border-gray-200 dark:border-gray-700 mb-4; }
      .modal-tab { @apply px-4 py-2 cursor-pointer border-none bg-transparent font-semibold text-gray-500 dark:text-gray-400 border-b-2 border-transparent -mb-0.5 text-sm transition-colors; }
      .modal-tab.active { @apply text-primary border-primary dark:text-primary-light dark:border-primary-light; }
      .tab-content { display: none; }
      .tab-content.active { display: block; }
      .search-results { @apply list-none p-0 my-2 max-h-48 overflow-y-auto; }
      .search-result { @apply px-3 py-2 cursor-pointer border-b border-gray-200 dark:border-gray-700 hover:bg-gray-50 dark:hover:bg-gray-700 transition-colors; }
      .search-result .food-name { @apply font-semibold text-gray-900 dark:text-gray-100; }
      .search-result .food-nutrients { @apply text-xs text-gray-500 dark:text-gray-400; }
      .nutrition-preview { @apply bg-gray-50 dark:bg-gray-700 p-3 rounded-lg mt-3; }
      .nutrition-preview .preview-grid { @apply grid grid-cols-3 gap-1 text-sm; }
      .nutrition-preview .preview-grid span { @apply whitespace-nowrap text-gray-700 dark:text-gray-300; }

      /* Barcode Scanner */
      #reader { @apply w-full max-w-lg mx-auto; }
      #reader video { @apply w-full rounded-lg; }

      /* Tables */
      .data-table { @apply w-full text-sm text-left; }
      .data-table thead th { @apply px-4 py-3 bg-gray-50 dark:bg-gray-800 text-gray-600 dark:text-gray-300 font-semibold text-xs uppercase tracking-wider border-b border-gray-200 dark:border-gray-700; }
      .data-table tbody td { @apply px-4 py-3 border-b border-gray-100 dark:border-gray-700/50 text-gray-700 dark:text-gray-300; }
      .data-table tbody tr:hover { @apply bg-gray-50/50 dark:bg-gray-800/50; }

      /* Form inputs */
      .form-input { @apply w-full px-3 py-2 rounded-lg border border-gray-300 dark:border-gray-600 bg-white dark:bg-gray-800 text-gray-900 dark:text-gray-100 focus:outline-none focus:ring-2 focus:ring-primary/50 focus:border-primary dark:focus:border-primary-light transition-colors; }
      .form-label { @apply block text-sm font-medium text-gray-700 dark:text-gray-300 mb-1; }

      /* Buttons */
      .btn-primary { @apply inline-flex items-center justify-center px-4 py-2 bg-primary text-white font-medium rounded-lg hover:bg-primary-hover focus:outline-none focus:ring-2 focus:ring-primary/50 transition-all active:scale-[0.98]; }
      .btn-secondary { @apply inline-flex items-center justify-center px-4 py-2 border border-gray-300 dark:border-gray-600 text-gray-700 dark:text-gray-300 bg-white dark:bg-gray-800 font-medium rounded-lg hover:bg-gray-50 dark:hover:bg-gray-700 focus:outline-none focus:ring-2 focus:ring-primary/50 transition-all active:scale-[0.98]; }
      .btn-danger { @apply inline-flex items-center justify-center px-4 py-2 bg-red-500 text-white font-medium rounded-lg hover:bg-red-600 focus:outline-none focus:ring-2 focus:ring-red-500/50 transition-all active:scale-[0.98]; }
      .btn-ghost { @apply inline-flex items-center justify-center px-3 py-1.5 text-sm text-gray-600 dark:text-gray-400 hover:bg-gray-100 dark:hover:bg-gray-700 font-medium rounded-lg transition-all; }
      .btn-ghost-danger { @apply inline-flex items-center justify-center px-3 py-1.5 text-sm text-red-600 dark:text-red-400 hover:bg-red-50 dark:hover:bg-red-900/30 font-medium rounded-lg transition-all; }
    }
  </style>
  <script>
    // Dark mode: check localStorage, then system preference
    (function() {
      var stored = localStorage.getItem('theme');
      if (stored === 'dark' || (!stored && window.matchMedia('(prefers-color-scheme: dark)').matches)) {
        document.documentElement.classList.remove('light');
        document.documentElement.classList.add('dark');
      }
    })();
  </script>
</head>
<body class="bg-gray-50 dark:bg-gray-900 text-gray-900 dark:text-gray-50 font-sans min-h-screen flex flex-col">
  <!-- Navigation -->
  <nav class="sticky top-0 z-50 bg-white dark:bg-gray-800 border-b border-gray-200 dark:border-gray-700 shadow-sm">
    <div class="max-w-6xl mx-auto px-4 sm:px-6">
      <div class="flex items-center justify-between h-14">
        <!-- Logo -->
        <a href="/" class="flex items-center gap-1.5 text-gray-900 dark:text-white font-bold text-lg hover:text-primary dark:hover:text-primary-light transition-colors">
          <img src="/pantrybot.png" alt="Pantry Bot" class="h-6 w-6">
          <span>Pantry Bot</span>
        </a>

        <!-- Desktop Nav Links -->
        <div class="hidden md:flex items-center gap-1">
          <% if (typeof user !== 'undefined' && user) { %>
            <a href="/" class="px-3 py-2 rounded-lg text-sm font-medium text-gray-600 dark:text-gray-300 hover:text-primary dark:hover:text-primary-light hover:bg-primary/5 transition-colors">Dashboard</a>
            <a href="/pantry" class="px-3 py-2 rounded-lg text-sm font-medium text-gray-600 dark:text-gray-300 hover:text-primary dark:hover:text-primary-light hover:bg-primary/5 transition-colors">Pantry</a>
            <a href="/recipes" class="px-3 py-2 rounded-lg text-sm font-medium text-gray-600 dark:text-gray-300 hover:text-primary dark:hover:text-primary-light hover:bg-primary/5 transition-colors">Recipes</a>
            <a href="/grocery" class="px-3 py-2 rounded-lg text-sm font-medium text-gray-600 dark:text-gray-300 hover:text-primary dark:hover:text-primary-light hover:bg-primary/5 transition-colors">Grocery</a>
            <a href="/nutrition" class="px-3 py-2 rounded-lg text-sm font-medium text-gray-600 dark:text-gray-300 hover:text-primary dark:hover:text-primary-light hover:bg-primary/5 transition-colors">Nutrition</a>
            <a href="/preferences" class="px-3 py-2 rounded-lg text-sm font-medium text-gray-600 dark:text-gray-300 hover:text-primary dark:hover:text-primary-light hover:bg-primary/5 transition-colors">Preferences</a>
          <% } else { %>
            <a href="/login" class="px-3 py-2 rounded-lg text-sm font-medium text-gray-600 dark:text-gray-300 hover:text-primary dark:hover:text-primary-light hover:bg-primary/5 transition-colors">Login</a>
            <a href="/register" class="btn-primary text-sm">Register</a>
          <% } %>
        </div>

        <!-- Right side: dark mode toggle + logout/mobile menu -->
        <div class="flex items-center gap-2">
          <!-- Dark Mode Toggle -->
          <button id="themeToggle" class="p-2 rounded-lg text-gray-500 dark:text-gray-400 hover:bg-gray-100 dark:hover:bg-gray-700 transition-colors" aria-label="Toggle dark mode">
            <!-- Sun icon (shown in dark mode) -->
            <svg class="hidden dark:block w-5 h-5" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
              <path stroke-linecap="round" stroke-linejoin="round" d="M12 3v1m0 16v1m9-9h-1M4 12H3m15.364 6.364l-.707-.707M6.343 6.343l-.707-.707m12.728 0l-.707.707M6.343 17.657l-.707.707M16 12a4 4 0 11-8 0 4 4 0 018 0z"/>
            </svg>
            <!-- Moon icon (shown in light mode) -->
            <svg class="block dark:hidden w-5 h-5" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
              <path stroke-linecap="round" stroke-linejoin="round" d="M20.354 15.354A9 9 0 018.646 3.646 9.003 9.003 0 0012 21a9.003 9.003 0 008.354-5.646z"/>
            </svg>
          </button>

          <% if (typeof user !== 'undefined' && user) { %>
            <!-- Logout (desktop) -->
            <form method="POST" action="/logout" class="hidden md:block">
              <input type="hidden" name="_csrf" value="<%= csrfToken %>">
              <button type="submit" class="btn-ghost text-sm">Logout</button>
            </form>
          <% } %>

          <!-- Mobile Menu Toggle -->
          <button id="mobileMenuBtn" class="md:hidden p-2 rounded-lg text-gray-500 dark:text-gray-400 hover:bg-gray-100 dark:hover:bg-gray-700 transition-colors" aria-label="Toggle navigation">
            <svg class="w-5 h-5" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
              <path stroke-linecap="round" d="M4 6h16M4 12h16M4 18h16"/>
            </svg>
          </button>
        </div>
      </div>

      <!-- Mobile Menu -->
      <div id="mobileMenu" class="hidden md:hidden pb-3 border-t border-gray-100 dark:border-gray-700 mt-1">
        <% if (typeof user !== 'undefined' && user) { %>
          <a href="/" class="block px-3 py-2 rounded-lg text-sm font-medium text-gray-600 dark:text-gray-300 hover:text-primary hover:bg-primary/5 transition-colors">Dashboard</a>
          <a href="/pantry" class="block px-3 py-2 rounded-lg text-sm font-medium text-gray-600 dark:text-gray-300 hover:text-primary hover:bg-primary/5 transition-colors">Pantry</a>
          <a href="/recipes" class="block px-3 py-2 rounded-lg text-sm font-medium text-gray-600 dark:text-gray-300 hover:text-primary hover:bg-primary/5 transition-colors">Recipes</a>
          <a href="/grocery" class="block px-3 py-2 rounded-lg text-sm font-medium text-gray-600 dark:text-gray-300 hover:text-primary hover:bg-primary/5 transition-colors">Grocery</a>
          <a href="/nutrition" class="block px-3 py-2 rounded-lg text-sm font-medium text-gray-600 dark:text-gray-300 hover:text-primary hover:bg-primary/5 transition-colors">Nutrition</a>
          <a href="/preferences" class="block px-3 py-2 rounded-lg text-sm font-medium text-gray-600 dark:text-gray-300 hover:text-primary hover:bg-primary/5 transition-colors">Preferences</a>
          <form method="POST" action="/logout" class="mt-1">
            <input type="hidden" name="_csrf" value="<%= csrfToken %>">
            <button type="submit" class="w-full text-left px-3 py-2 rounded-lg text-sm font-medium text-gray-600 dark:text-gray-300 hover:text-red-600 hover:bg-red-50 dark:hover:bg-red-900/20 transition-colors">Logout</button>
          </form>
        <% } else { %>
          <a href="/login" class="block px-3 py-2 rounded-lg text-sm font-medium text-gray-600 dark:text-gray-300 hover:text-primary hover:bg-primary/5 transition-colors">Login</a>
          <a href="/register" class="block px-3 py-2 rounded-lg text-sm font-medium text-primary dark:text-primary-light hover:bg-primary/5 transition-colors">Register</a>
        <% } %>
      </div>
    </div>
  </nav>

  <script>
    // Mobile menu toggle
    document.getElementById('mobileMenuBtn').addEventListener('click', function() {
      document.getElementById('mobileMenu').classList.toggle('hidden');
    });
    // Dark mode toggle
    document.getElementById('themeToggle').addEventListener('click', function() {
      var html = document.documentElement;
      if (html.classList.contains('dark')) {
        html.classList.remove('dark');
        html.classList.add('light');
        localStorage.setItem('theme', 'light');
      } else {
        html.classList.remove('light');
        html.classList.add('dark');
        localStorage.setItem('theme', 'dark');
      }
    });
  </script>

  <!-- Main Content -->
  <main class="flex-1 max-w-6xl w-full mx-auto px-4 sm:px-6 py-8">
    <% if (typeof flash !== 'undefined' && flash && flash.length > 0) { %>
      <% flash.forEach(function(msg) { %>
        <div class="flash flash-<%= msg.type %>"><%= msg.message %></div>
      <% }); %>
    <% } %>
